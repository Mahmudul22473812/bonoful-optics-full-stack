export type Address={id?:string;label?:string;name:string;phone:string;line1:string;line2?:string;city:string;postalCode:string;country:string;isDefault?:boolean};
export function AddressFields({prefix='',value}:{prefix?:string;value?:Partial<Address>}){return <div className="form-grid"><label>Full name<input name={`${prefix}name`} defaultValue={value?.name} required minLength={2} maxLength={100} autoComplete={`${prefix?'billing':'shipping'} name`}/></label><label>Phone<input name={`${prefix}phone`} defaultValue={value?.phone} required minLength={7} maxLength={25} autoComplete={`${prefix?'billing':'shipping'} tel`}/></label><label className="span-two">Street address<input name={`${prefix}line1`} defaultValue={value?.line1} required minLength={5} maxLength={200} autoComplete={`${prefix?'billing':'shipping'} address-line1`}/></label><label className="span-two">Apartment / area (optional)<input name={`${prefix}line2`} defaultValue={value?.line2} maxLength={200} autoComplete={`${prefix?'billing':'shipping'} address-line2`}/></label><label>City<input name={`${prefix}city`} defaultValue={value?.city??'Dhaka'} required minLength={2} maxLength={80}/></label><label>Postal code<input name={`${prefix}postalCode`} defaultValue={value?.postalCode} required minLength={2} maxLength={12}/></label></div>;}
export function readAddress(data:FormData,prefix=''):Address{return {name:String(data.get(`${prefix}name`)),phone:String(data.get(`${prefix}phone`)),line1:String(data.get(`${prefix}line1`)),line2:String(data.get(`${prefix}line2`)??''),city:String(data.get(`${prefix}city`)),postalCode:String(data.get(`${prefix}postalCode`)),country:'BD'};}
